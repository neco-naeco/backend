export enum GameRoomMissionStepStatus {
  LOCKED = 'LOCKED',
  READY = 'READY',
  IN_PROGRESS = 'IN_PROGRESS',
  CLEARED = 'CLEARED',
  FAILED = 'FAILED',
}

export enum MissionResultJudgeStatus {
  PASSED = 'PASSED',
  FAILED = 'FAILED',
  ERROR = 'ERROR',
}
